# 变更记录

## 0.0.0.5

把**英文**的设置界面补上（日文在 0.0.4 已做）。动作名四语早已自动解决，
这一版补的是菜单栏供不上的那部分：设置界面各页的选项行、存档界面、底部按钮。

**`data/a11y_labels_en.ini`**

- 取证方法同日文：把界面语言切到 English，逐屏截图对着控件坐标逐项抄（`en_02_set0..7`、
  `en_03_save`、`en_00_title`），**不是翻译**。
- 覆盖：8 个页签 + Display / General 1 / General 2 / Text / Sound / Dialog 六页的全部选项行、
  Mouse 页的命令面板 20 格、Shortcuts 页里菜单栏供不上的三组（ctrl / hide / volume）、
  存档界面、底部按钮（`back=Return` / `title=Title Screen` / `reset=Defaults`）。
- 实机验证：`a11y_labels: 共 N 条（lang=en）`，页签念出
  `Display / General 1 / General 2 / Text / Sound / Dialog / Mouse / Shortcuts`，
  Shortcuts 页念出 `Save primary key / Ctrl skip primary key …`。
  （`Save` 来自菜单 caption，`Ctrl skip …` 来自这份表 —— 两条来源都生效。）

**故意没写的两处（宁可缺，不编）**

- `ch6` / `sysvo6`（駒川 みづは）：英文截图里那一格没看清，**不写**，保持中文退回，
  等重新截图确认再补。日文那边是有依据的（`ch6 = 駒川 みづは`），所以映射本身确定，
  缺的只是英文拼写。
- `home` / `end` / `slider`（回忆界面的滚动控件）：没有英文截图证据，同样不写。

**已知未完成**

- **繁体（tw）表还没出。** 方法已经跑通：`tools\capture_ui.ps1 -Tag tw` 抓 12 张 → 逐屏抄。
  另外繁体**不能**用 `StrConv` 生成（会把「游戲」转成「游戲」，应为「遊戲」）。
- 自定义按钮排列界面（`item0-19` / `pad0-18` / `func0-19`）四语都还是中文位置说法。

## 0.0.0.4

控件标签跟着游戏语言走。上一版只让**插件自己的话术**跟语言走，控件标签仍是写死的中文。

**先纠正一个我自己搞错的模型：游戏有「三套」语言，不是两套**

菜单栏 `テキスト言語(&L)` 展开后能直接看到（实机截图 + 程序枚举都对上了）：

```
[メイン] 日本語 / English / 簡体中国語(✓) / 繁体中国語      ← 主要语言
[サブ]   日本語 / English / 簡体中国語 / 繁体中国語 / なし(✓) ← 字幕语言
```

| | 谁管 | 内部字段 |
|---|---|---|
| 主要语言 `[メイン]` | **剧情正文** | `CurrentLanguageTag` / `kag.languageType` |
| 字幕语言 `[サブ]` | 剧情第二语言（双语同显） | `SystemConfig.subLanguageType`（`-1`=なし） |
| **界面语言** | **菜单栏・界面图片・控件** | **`CurrentLanguageUITag` / `uiLanguageType`** |

关键证据：主/字幕两项**都归在「テキスト言語（文本语言）」这一个菜单里**；实测
Main=簡体中国語、Sub=なし 的同时 `uitag=jp`，而标题菜单念「はじめから／つづきから」、
原生菜单栏是「ファイル(&F)／セーブ(F2)」—— **控件跟的是界面语言**。
标题右上角那个 `Language:` 按钮设的就是界面语言。

> 第一版误用了 `CurrentLanguageTag`（主要语言 = 剧情那套），界面切成英文时插件仍念中文
> 标签。已改为 `CurrentLanguageUITag`；又因为 V2Link 阶段界面语言可能还没定下来
> （实测那时读到空值、回退成了主要语言），改成**每次扫描重问一次**直到确认。

**动作名：不写数据文件，直接读游戏原生菜单栏的 caption**

标题右上角一改界面语言，`global.kag.menu`（原生 Win32 菜单栏）的 caption
**立刻**跟着变，而且四语齐全（实测）：

```
uitag=jp → セーブ(F2) / バックログ(&L)          / [メイン] 日本語(&J)
uitag=en → &Save(F2) / Back&log                / [Main] &Japanese
uitag=tw → 保存進度(F2) / 歷史記錄(&L)          / [主要] 日語(&J)
```

所以工具栏那 23 个动作名**不需要任何数据文件**，问游戏要就行 —— 永远是它自己的用词、
永远跟着界面语言走。实测 `menu labels: 23 条`，标题菜单念出
`言語 / はじめから / つづきから / 前回のつづきから / フローチャート / システム設定 / ゲームの終了`。
繁体也因此**不用自己做简繁转换**（`StrConv` 会把「游戲」转成「游戲」，游戏给的是「保存進度」）。

**设置界面的标签：按界面语言分文件**

- 新增 `plugin\a11y_labels_<界面语言标签>.ini`（`jp` / `en` / `cn` / `tw`），
  在通用 `a11y_labels.ini` **之后**加载，优先级最高。
- 内建表保持 cn（逐条实机核对过），其它语言各放一个数据文件 ——
  可以增量补、随时改，都不用重新编译。

**日文表 `data/a11y_labels_jp.ini`（289 条）**

- 取证方法：把**界面语言切到「日本語」逐屏截图**，对着控件坐标逐项抄 —— 不是翻译。
- 覆盖：标题菜单 / 设置 8 个页签及全部选项 / 键盘页 24 个动作的主副键 /
  鼠标页 20 个手势功能 / 存档读档界面 / 回忆界面 / 底部工具栏。
- 实机验证：`language tag=jp`、`a11y_labels: 共 289 条（lang=jp）`、`menu labels: 23 条`。

**可复用的取证方法**

- 界面语言**只改内存、不重绘界面图片**，被强杀还不写配置 —— 要真的换界面得走
  标题右上角那个 `Language:` 按钮（会正常写配置）。
- `tools\capture_ui.ps1 -Tag <jp|en|tw>` 一次抓齐 12 张
  （标题 / 底部工具栏 / 设置 8 页 / 存档界面 / 回忆界面），再用同一套坐标对照抄。

**顺带纠正一处用词**

- 按日文截图：`skip` 那一格游戏自己写的是「スキップモード」，中文界面同一行写「快速模式」；
  「快进」是 `backskip` 那一格。内建表里 `skip` 从「快进」改成「快速模式」，
  `backskip` 改成「快进」。

**这一轮查清的事实（决定了后面的工作量）**

- **设置界面**的选项文案是按语言预渲染的图片：`uipsd/<语言>/<界面>_<语言>.pimg`
  （`sys_page0_jp.pimg`、`sl_save_tw.pimg` …），没有文本可抠；
  `main/syslangtext_cn.ini` 与 `uipsd/_func/*.func` 解包出来都是**加密的**，
  运行时 `global.SystemLanguageText` **不能 for-in**（不可捕获地终止脚本）。
  所以这部分只能逐屏截图取证。
- 但**动作名不必** —— 原生菜单栏就是现成的四语文本源（见上）。

**踩到并修掉的 TJS 脚本坑**

- **`new Array("a","b")` 带参数会不可捕获地终止整段脚本**（`new Array()` 空参一直没事，
  之前所有脚本都是空参，所以一直没暴露）。改用数组字面量 `["a","b"]`。
- 探测脚本里 `function f(){…}` 在 REPL 里**时灵时不灵**，全部改成内联。
- 话术表 `g_phrases[]` **又一次与 enum 错位**（`P_KEY*` 加在 `P_LOADED` 之前），
  界面上把「主键」当成了启动语念出来。表头那句「两处必须同顺序」的警告是我自己写的，
  这次是我自己踩的。

**中途换语言也要跟着换（玩家会反复切来切去）**

- 语言标签随**已有的 5Hz 轮询**一起捎回来（`CurrentLanguageUITag` 作为轮询结果最后一节），
  C++ 侧只做节流：**最快 800ms 认一次**。不额外发任何 TJS 调用。
- 认到变化就重新做四件事：重抓菜单 caption → 重选 `a11y_labels_<tag>.ini` →
  重建生成式别名 → 正在导航就重扫一遍并播报「界面已更新」。
- 实机验证（导航模式中，用 `SetUILanguageType` 切 jp→en）：
  ```
  language changed -> en，重新加载标签
  menu labels: 23 条
  a11y_labels: 共 0 条（lang=en）
  [朗读] Screen updated, 界面语言。1 / 7     ← 播报语也跟着变
  [朗读] Load。3 / 7
  [朗读] FlowChart。5 / 7
  ```

**`data/a11y_labels_en.ini`（部分表）**

- 菜单栏里**没有**对应项的那几个（`start` / `continue` / `language` / `system`）
  菜单 caption 供不上，会退回内建中文、念出来中英混着。这一份把标题菜单补齐。
- 实测（`SetUILanguageType` 切到 en 后）：
  `Language / START / Load / CONTINUE / FlowChart / SETTINGS / Close Game`。
- 文件头写明了它是**部分表**：动作名不在这里也不需要（走菜单 caption，四语自动）。

**已知未完成**

- **英文（en）与繁体（tw）的设置界面标签还没出。** 动作名已由菜单 caption 自动解决，
  标题菜单的英文已补；缺的是设置界面那些选项行与角色名。
- 日文表未覆盖：自定义按钮排列界面（`item0-19` / `pad0-18` / `func0-19` 仍是中文位置说法）。

## 0.0.0.3

三处调整（都不是修 BUG），外加顺带发现并修掉的几处。

**一、存档位读出「有没有存档」**

- 全部走游戏自己的 bookmark API，一个数字都不猜：
  `kag.getBookMarkFileNameAtNum` / `getBookMarkPlayTime` / `getBookMarkPageName` / `getBookMarkDate`。
- **快存槽数与每页槽数是数出来的，不是写死的**：从 0 往上数文件名以 `data_quick_` 开头的
  有几个（实测 12），接着数 `data_0001_` 开头的有几个（实测 12）。当前页取
  `global.Current.page`。文件名实测：`data_quick_01..12` → `data_0001_01..12` → `data_0002_01…`。
- 空 / 有存档用 `getBookMarkPlayTime(n) > 0` 判断 —— **纯数值判据，不含任何文案**。
  有存档时直接念游戏给的章节名与日期。实测：
  `存档位 1：chapter　3-2，2026/09/25 19:47` / `存档位 2：空`，与实机截图逐格一致
  （截图里只有 `0001-01` 有缩略图，其余全是 `NO DATA`）。
- 踩坑记录：**不要用 `getBookMarkDateText(n)`** —— 它对任何槽位都只返回
  `1970/01/01 08:00` 这个无效默认值，真日期在 `getBookMarkDate(n)` 里。

**二、有配音的台词不再自动朗读，但退格仍可重读**

- 判据来自游戏数据：`kag.historyLayer.currentInfo.voice` 是 Object 就表示这一句有配音
  （`backlog.tjs` 的 `setNewAction` 里 `spde currentInfo.voice` 写入），undefined 表示没有。
- 有配音 → 不再叠一层 TTS（人声已经在读了），但 `g_lastText` / `g_lastAnnounced`
  照常更新，所以**「退格重读这一句」完全不受影响** —— 配音只影响自动朗读，不影响按需朗读。
- 实测：`voiced -> tts suppressed: 「当然了，我也想跟她们搞好关系……」`，
  随后按退格念出 `廉太郎，「当然了，我也想跟她们搞好关系……」`。

**三、文案跟着游戏的语言走**

- 启动时读一次 `global.CurrentLanguageTag`（实测 `cn`），插件自己说的话不再写死中文。
- 24 条自有话术按 **jp / en / cn / tw 四列**存放，按当前语言标签取。cn 列逐条实机核对过；
  jp / en / tw 是意译，**不保证与官方用词一致**，想改不用重编译
  （`plugin\a11y_labels.ini` 可覆盖标签，话术表在源码 `g_phrases` 里）。
- 凡是**游戏自己就有**的文案一律用游戏返回的原字，不自己造：存档的章节名与日期即是一例。

**顺带发现并修掉的**

- **存档网格第 4 行是画面上看不见的假控件。** `ParentHackLayer` 高 720，而
  `item30..item33` 的 `y=738` 整行落在容器外，`visible` 却仍是 1 ——
  收进导航就是一个点不到的假项。扫描现在按父容器矩形裁剪子控件。
- **翻页按钮方向反了。** 实测位置是 `page_top(523) page_dw10(565) page_dw1(607)`
  `[slider] page_up1(905) page_up10(947) page_end(989)`，画面上左侧是「往前」、
  右侧是「往后」，所以 `page_up*` 是**往后**翻。旧标签把 `page_up1` 写成「上一页」，
  方向是反的。已按实测位置改正，并补上 `page_top` / `page_dw1` / `page_dw10` /
  `page_add`（增加一页）/ `page_del`（减少一页）/ `page_num` / `page_text`。
- `ParentHackLayer`（存档槽定位容器）与 `detail`（读档界面左侧详情面板）不再是导航项。
- **文案表与 enum 错位**：把 `P_PAGE` 加进 enum 中间、表行却加在 `P_SLOT` 后面，
  导致其后每条话术整体移一位，界面上直接念出了「第 %d 页」。已修正，
  并在源码表头写了「两处必须同顺序」的警告。

## 0.0.0.2

界面覆盖：回忆（历史记录）、设置界面全部 8 个页签、启动「注意」声明、工具栏补全。

**回忆（历史记录）界面**

- 正文取自 `kag.historyLayer.data`（本机实测 26 条）。同一条上：`.text` 是**中文**、
  `.plaintext` 是日文原文、`.disp` 是**中文说话人**（`.name` 是日文 `運転手`，不能用）。
- 否掉了一条死路：界面上那 5 个正文块层的 `blockInfo.text` **只有日文**，
  走它读不出中文（截图上是中文，译文是绘制时才套上的）。
- 逐句朗读，进界面默认停在**最新一句**（正文按时间从旧到新排）。
  正文不可点击：回车只重念 —— 消息窗那一片底下就是「推进剧情」的命中区。
- 实机验证：`history: 26 句（共 26 句）`、`scan: 34 个控件`，逐句与截图一致。

**设置界面 8 个页签**

- 把 page1..page7 的子控件全量转储 + 逐页截图对照，补齐约 150 条标签。
- 成对控件的层名后缀就是它设的标志值，**已用实点验证**：
  点 `afterskip_off` → `kag.afterskip` 变 0；点 `afterskip_on` → 变 1。
- 键盘页 24 个动作与鼠标页 20 个功能共用同一份中文文案，用生成式别名一次生成
  `<动作>_key1` / `<动作>_key2` / `cp_<动作>`（共 148 条），不用抄三遍。
- 全部滑条（音量 9 条 + 速度 4 条 + 不透明度 2 条 …）纳入左右方向键调节。

**启动「注意」声明**

- 实测它是**一张整屏图片**（`image/attention_cn.png`，按语言分版本）挂在 `SysCoverLayer` 上，
  没有任何文本可读，所以由插件自己念一段固定说明，并给导航模式一个「按回车关闭」的入口。
- 实机验证：启动日志 `cover announced` + 全文朗读。

**工具栏**

- 补齐 `prev` `prevscn` `backskip` `backone` `nextscn` `next` `vreplay` `vsave` 八个标签，
  并把 `log` / `scnchart` / `volchg` / `hide` 改成游戏自己的说法
  （打开历史记录 / 打开流程图 / 音量调整 / 隐藏对话框）。

**修掉的问题**

- 同一块地方同时被「容器子控件」和「网格叶子」命中时只留子控件 ——
  设置界面底部原来会连着念两遍「回到游戏」。
- `TouchUiLayer:*`、`ShowDateLayer:*` 这类「父层:子层」的内部层不再算控件
  （ADV 里曾因此多出 3 个噪声项）。
- 生成式别名里**名字序号与文案序号一度共用同一个 `%d`**：`gesture%d` 生成的是
  `gesture1..gesture5`（漏掉 `gesture0`，实测就是它没被翻译），`item` 的文案则成了「第 0 格」。
- `data/a11y_labels.ini` 从「照抄一份内置表」改成**纯模板**。它本来就会**覆盖**内置表，
  抄一份的后果是内置表更新了、这里还留着旧说法反而把新标签盖回去 ——
  0.0.0.2 实测踩到：`slider=捲動軸`、`custom=自定义` 把新标签盖掉了。
  顺带把读档/存档界面的文案从繁体改成简体（本作跑的是 CN 版）。

**已知未完成**

- **自定义按钮排列界面**：排列条 `item0-19`、槽位 `pad0-18`、按钮库 `func0-19`
  只做位置说法（「按钮库第 3 格」），**图标与功能的对应关系没有确认** ——
  游戏把它存在 `SystemConfig` 的 `qmorder_<type>` 里，实测这几个键都取不到值。
- 回忆界面的 `jump0-4`（跳到这一句）、`vreplay0-4`（重播语音）、`vsave0-4`（收藏语音）
  是位置相关按钮，暂时不做成导航项。
- 显示类选项的**当前值**仍读不出来（同 0.0.0.1）。
- 设置·键盘页画面上写的是「快速模式」，插件统一念「快进」；想改可用
  `plugin\a11y_labels.ini` 覆盖（`skip_key1=快速模式 主键`）。

## 0.0.0.1

首个版本。单进程 V2Link 插件，语音直接进读屏 DLL，界面用「按键 → 真实点击」的方式强行开通键盘导航。

**语音**

- 后端链：Tolk（可选）→ `nvdaControllerClient.dll` → SAPI5（纯 vtable，不走 COM 后期绑定）。
  任一级失败只降级，不把异常抛回游戏主循环。
- 实测日志确认：`语音后端: NVDA`。

**剧情朗读**

- 文本取自 `kag.historyLayer.currentInfo`。
- 修正「选项念成日文」：普通台词与选项行的 `.text` / `.plaintext` 含义**是反的**
  （普通台词 `.text` 是译文、`.plaintext` 是原文；选项行正好相反）。
  判据用「有没有假名」，不写死字段名 —— 含假名的判为原文，另一个才是译文。
  实机验证：选项念出「选项 2 个。1，还是觉得城市好。2，不好说。按数字键选择。」

**选项**

- 选项清单取自 `kag.selectLayer.selects[i]`（`name` / `left` / `top` / `width` / `height`）。
- 数字键 `1`–`9` 直接选，点的是**游戏自己给的按钮矩形中心**，不是猜的布局。
  实机验证：`已选择 不好说` → 点击 `(960,463)` → 剧情进入对应分支。

**界面导航**

- 控件发现：对逻辑分辨率打 40px 网格，问 `kag.getPrimaryLayerAt(x,y)`「这一点上是哪一层」，
  再枚举该层的 `children`（子控件坐标相对父层，绝对坐标 = 父层 + 子控件）。
- 修复一处硬伤：早先有 `if(w>=1900)continue;` 把**整屏容器**整层丢掉，而设置界面的选项
  全部是宽 1928 的 `表メッセージレイヤ1` 的子控件 —— 于是四十多个子控件一起被丢，
  每个选项只能碰到被网格恰好命中的那一半（显示模式只念到「全屏」念不到「窗口」，
  老板键功能、显示说话人表情整行消失）。修后同一界面 **22 项 → 40 项**，每半都有精确坐标。
- 控件按「先上后下、先左后右」排序（阅读顺序），不再按图层树的发现顺序乱跳。
- 标签表：内置表 + `plugin\a11y_labels.ini` 覆盖，**念错不用重编译**。
  设置界面的页签标签按实机截图改正（原先写成「通用1/通用2」，实际是
  「画面显示 / 游戏设置1 / 游戏设置2 / 文本 / 音频 / 确认 / 鼠标 / 键盘」）。
- 读档界面：16 个存档槽是 `ParentHackLayer` 的子控件 `item00..item33`（相对坐标 252x228），
  已能导航与激活。实机验证：激活「存档位 1」→ 画面上第一格被选中。

**快进**

- 游戏本体**收真键盘**（走 DirectInput / 键盘状态轮询，不经过窗口消息），
  原生键位：空格/回车推进、`R` 历史、按住 `Ctrl` 快进。
- 模式在原生快进之上补自己的点击，好处不是更快（实测引擎把推进节流在 ~2.5 句/秒，
  点 17.8 下/秒也只到 2.3 句/秒），而是：快进期间不朗读过路台词、
  一旦检测到选项立刻停手交给朗读、松手时补读当前这一句、不在剧情里时只试推 3 下就收手
  （读档画面的存档格正好压在推进点上，连点变双击会读档）。

**已知未完成**

- 设置界面里显示类选项（显示模式 / 总在最前 / 功能区域 / 时间滑条 …）的**当前值读不出来**，
  所以念得出「显示模式：窗口 / 全屏」，念不出当前选的是哪一个。
  只有 7 项能读：`kag.afterskip` `afterauto` `voicecut` `voicecutpage` `bgmdown`
  `noeffect` `sysnoeffect`。
- 读档界面缺「下一頁」（`page_add`）。
- 设置界面里滑条（章节标题显示时间 / 背景音乐曲名显示时间）目前只按百分比点击，
  还没有念出游戏实际显示的值。
- 设置界面底部「回到游戏」按钮的标签不对：同一个按钮在不同界面用了不同层名
  （设置界面里叫 `title2`，而 `title2` 在别的界面是「回到标题界面」）。
  需要「按位置限定」的标签能力才能修准，还没做。

**开发中踩到并修掉的两个坑**（都留下了教训，见 `docs/DESIGN.md`）

- **坐标必须按浮点解析。** 扫描器发的是控件中心，宽高为奇数时是 `167.5` 这种半像素值；
  用 `"%d,…"` 解析时 `%d` 读到 `167` 撞上 `.` 就整个中止，后面 `y/w/h` 全部留 0。
  后果有两层：排序键全烂（标题菜单念成 开始→退出→流程图→系统设置→继续→语言→读取），
  **以及激活时点到 `(x,0)` 即屏幕最顶端 —— 看起来就是「点了没反应」。**
- **拆字段时必须先把制表符打成字符串结尾。** 少了 `*tab = 0;`，层名会连父层名一起带走
  （`"helpbase\t…,表メッセージレイヤ0"`），而噪声过滤规则是「名字里含 レイヤ 就丢」——
  父层名里的 レイヤ 被带进来，**每一项都被丢掉**，表现为扫描恒返回 0 项、
  导航模式里按方向键一片死寂（`g_itemCount == 0` 时方向键直接 return，什么都不念）。

